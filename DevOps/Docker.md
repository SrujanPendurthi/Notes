Docker containers are very similar to Virtual Machines(VMs) as they provide isolation, control, and reproductibilty across different devices

Benefits of Docker Containers over VMs:
	faster startup time
	minimal overhead
	far smaller data usage
	near-native performance

